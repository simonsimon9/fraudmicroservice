spring:
  application:
    name: eureka-server
  zipkin:
    base-url: http://localhost:9411
server:
  port: 8761
eureka:
  client:
    fetch-registry: false #false cause this server itself
    register-with-eureka: false #false cause this is the server itself
